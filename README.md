# WordPress Plugin Base Project
 written in PHP and JS

# Plugin introduces functionality
    currently we can alter database tables
    started with users table.

## bugs
- need to use prefix instead of db table exact name
